
export const userFiat = ({ currency }) => currency.currentFiat;
export const currentRate = ({ currency }) => currency.currentRate;

export const walletData = ({ currency }) => currency.currentWallet;


